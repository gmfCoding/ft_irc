#include "PrivmsgCommand.hpp"

int privMsgCommand(IRCClient* client, const std::vector<std::string>& parameters){
    //check if they have perms

    //params are <receiver>{,<receiver>} <text to be sent>

    //match client to client

    //send the msg to the user if no errors so far
    
    //cmd success or fail code for return;

}